#ifndef CONSOLE_HPP_
#define CONSOLE_HPP_

#include <string>
#include <map>
#include <vector>
#include "Sorting.hpp"

class Console
{
    public:
		Console() = default;
		~Console() = default;

        bool startConsole();

    private:

};

#endif // CONSOLE_HPP_