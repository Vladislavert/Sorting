#include <sstream>
#include <vector>
#include <boost/algorithm/string.hpp>

#include "Console.hpp"

#include "Sorting.hpp"

int main()
{
	Console console;
	while (console.startConsole()) {}

	// test quickSort

//	int a[] = {1, 10, 5, 13};
//
//
//	std::cout << "array" << std::endl;
//	sort::display(std::begin(a), std::end(a));
//
//	sort::quickSort(std::begin(a), std::end(a));
//	std::cout << "sort array" << std::endl;
//	sort::display(std::begin(a), std::end(a));

	return (0);
}